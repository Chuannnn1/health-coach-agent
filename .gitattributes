# Windows batch files MUST use CRLF or cmd.exe parses them wrong
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Shell scripts must stay LF
*.sh  text eol=lf
mvnw  text eol=lf

# Default: let git normalize
*     text=auto
